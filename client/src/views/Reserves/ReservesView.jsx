import React from "react";

export default function Reserves(){
 return(
    <div className="">
        {lang === 'es' ? (
                    <h1>Reservas</h1>

        ):(
                    <h1>Reserves</h1>

        )}
    </div>
 )
}

